# Checkerboard Calibration Images

Place manually captured checkerboard images here while running `notebooks/01_camera_calibration.ipynb`.

Delete the images after `outputs/calibration_intrinsics.json` has been verified. These images are not part of normal field data collection.
